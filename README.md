ZaiChat with Ionic/Capacitor
============================

First of all
```sh
$ npm install

```

Run in the browser
```sh
$ npx ionic serve
```

Run on Android
```sh
$ npx ionic build
$ npx capacitor run android
```
