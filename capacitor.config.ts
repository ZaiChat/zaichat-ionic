import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.zaichat.ionic',
  appName: 'zaichat-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
